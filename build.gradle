group 'prv.saevel'

version '1.0'

subprojects {

    ext {
        java = ["version" : "1.8"]

        drools = ["version" : '7.28.0.Final']

        spring = ["version" : "2.2.0.RELEASE"]

        junit = ["version" : "4.12"]

        lombok = ["version" : '1.18.10']
    }
}
